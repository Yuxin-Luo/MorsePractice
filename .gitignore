# Dependencies
node_modules/

# Test output
coverage/
*.lcov
.nyc_output/

# Editor / OS
.DS_Store
*.log
.vscode/*
!.vscode/extensions.json
.idea/

# Build artifacts
dist/
build/

# Reference repos (kept locally, not in version control)
ReferenceRepositories/

# Local env
.env
.env.local

# Marketing / social copy drafts (personal, not for the repo)
marketing/

# Android signing keys (NEVER commit)
*.keystore
*.keystore.b64

# Sensitive keys & credentials (NEVER commit)
mykEY/
